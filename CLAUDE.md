# קל לי סטניקס בבית — הנחיות לעבודה על הפרויקט

## פריסה: למזג ל‑main בכל עדכון

האתר מחובר ל‑Netlify דרך הענף `main`, וכל דחיפה ל‑main מתפרסמת אוטומטית
לאפליקציה שמותקנת על הטלפון של המשתמש.

לכן, **אחרי כל שינוי שנדחף לענף העבודה, למזג אותו ל‑main ולדחוף** — בלי
לשאול, זו בקשה קבועה של המשתמש:

```bash
git push -u origin <ענף-העבודה>
git checkout main && git merge --no-edit <ענף-העבודה> && git push origin main
git checkout <ענף-העבודה>
```

תמיד לחזור לענף העבודה בסוף, ולוודא ש‑main לא נשאר מאחור.

## מבנה

אפליקציית PWA סטטית, ללא שלב בנייה וללא תלויות. `index.html` בשורש.

- `exercises.js` — מאגר התרגילים (שם עברי, שם אנגלי, שלבי ביצוע, דגש טכניקה)
- `program.js` — מנוע התוכנית: שמונה תרגילים קבועים, יעד (חזרות או שניות) לכל
  תרגיל, חישוב זמנים
- `app.js` — ניווט, נגן האימון, מעקב התקדמות, הגדרות
- `img/` — איור לכל אחד מ‑14 התרגילים שהתוכנית מגישה (השמונה של הסבב,
  שלושה חימום ושלושה שחרור), `<id>.webp` ברקע שקוף, 400x400
  (פי 2 לתצוגה של 200x200). דחוס ללא אובדן — הפיקסלים זהים למקור
- `exercises/` — קובצי המקור שהמשתמש מספק, PNG בשמות התרגילים באנגלית.
  ההמרה היא שינוי פורמט ושם בלבד: אין לחתוך, למרכז מחדש או לנקות אותם
- `source/exercises-full.js` — מאגר 118 התרגילים המלא. `exercises.js` שנשלח
  לדפדפן מכיל רק את 14 התרגילים שהתוכנית מגישה בפועל
- `source/exercises-sheet.png` — גיליון ישן שממנו נחתכו האיורים הראשונים
- `sw.js` + `manifest.webmanifest` + `icons/` — PWA
- `_headers` — הגדרות Netlify (ה‑Service Worker לא נשמר במטמון)
- `.htaccess` / `nginx.conf.example` — אותן הגדרות לאחסון עצמי (Apache / nginx)

## כללים שחשוב לשמור

- **עברית ו‑RTL**: כל הממשק בעברית. חצי פעולה מצביעים שמאלה (התקדמות ב‑RTL).
  שמות התרגילים מוצגים בעברית ומיושרים לימין, עם השם באנגלית מתחתיהם.
  ההקראה הקולית של שם התרגיל היא באנגלית (`en-US`) — זו הפרדה מכוונת.
- **משך מדויק**: האימון חייב להסתיים בדיוק במספר הדקות שהוגדר. לאחר כל שינוי
  במנוע התוכנית, לוודא שאין סטייה בכל שילוב של משך, רמה ושבוע. שימו לב:
  לכל תרגיל זמן עבודה משלו, אז אסור להכפיל בזמן אחיד — חייבים לסכום
  תרגיל‑תרגיל, גם בחישוב סבב הסיום.
- **היעדים**: ברמה בינונית בשבוע הראשון היעדים הם בדיוק אלה שבתוכנית שהמשתמש
  בחר — 12, 15, 45״, 12, 30״, 12, 15, 12. לא לשנות בלי בקשה מפורשת.
- **גרסת ה‑Service Worker**: להעלות את `VERSION` ב‑`sw.js` בכל שינוי בקבצים.
  זה מה שמפעיל את הודעת העדכון בתוך האפליקציה — בלי העלאת מספר, המשתמש לא
  יידע שיש גרסה חדשה ויישאר עם הקבצים מהמטמון. כדאי לעדכן גם את
  `APP_VERSION` ב‑`app.js` לאותו מספר.
- **האיורים**: לכל תרגיל שהתוכנית מגישה חייב איור ב‑`img/<id>.webp`, ושם הקובץ
  הוא מזהה התרגיל. `PROGRAM.hasImage` נגזר מהסבב, מהחימום ומהשחרור יחד — מי
  שמוסיף תרגיל חייב גם איור, אחרת הריבוע יופיע ריק. הריבוע במסך הוא 200x200
  קבוע, ו‑`object-fit: contain` שומר על יחס הצדדים בלי מתיחה.
- **משקל**: כל מה שנכנס לרשימת ה‑`ASSETS` ב‑`sw.js` יורד ונשמר במטמון בהתקנה
  הראשונה. אייקוני PNG עוברים דרך `pngquant` אחרי הרינדור מה‑SVG, ואין לשלוח
  לדפדפן נתונים שהאפליקציה לא מגישה.
- **שחזור אימון שנקטע**: נקודת העצירה נשמרת ב‑`calisthenics.home.resume`
  ונבנית מחדש עם הרמה, השבוע והמשך ששימשו אז — לא עם ההגדרות הנוכחיות.
  הצעת ההמשך קודמת לכל חלונית אחרת (`resumePending`), כי יש רק חלונית אחת.
- **בלי נוטיפיקציות ותזכורות** — דרישה מפורשת של המשתמש.

## בדיקות

אין חבילות התקנה. לבדיקה מריצים שרת סטטי ופותחים בדפדפן ברוחב של טלפון:

```bash
npx http-server -p 8765 -s .
```
